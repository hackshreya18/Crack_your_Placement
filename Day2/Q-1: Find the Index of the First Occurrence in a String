class Solution {
public:
    int strStr(string haystack, string needle) {
        int ans=haystack.find(needle);

        if(ans<haystack.size()){
            return ans;
        }
        return -1;
    }
};
